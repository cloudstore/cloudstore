/**
 * SSL-specific stuff needed to secure the connection used by the REST client.
 * <p>
 * Even though SSL is now officially called TLS, SSL is still better known - and many classes in the JRE
 * still use SSL, too.
 */
package co.codewizards.cloudstore.rest.client.ssl;